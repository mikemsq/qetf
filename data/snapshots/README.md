# data/snapshots/

Versioned dataset snapshots used for reproducibility.

A snapshot is an "as-of" bundle that makes a backtest deterministic:
- the exact curated tables used
- the "as-of" timestamp for metadata and universe membership
- any symbol mapping used
